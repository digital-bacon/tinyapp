<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <%- include('partials/_bootstrapCSS') %>
    <title>Register - TinyApp</title>
  </head>

  <body>
    <%- include('partials/_header') %>

    <main style="margin: 1em;">
    <div class="row">
      <div class="col-sm-6 mt-2 mx-auto">
        <div class="card">
          <div class="card-body">
            <h3>Log in to your account</h3>
            <div class="alert alert-warning">
              <p class="m-0">This is a learning app intended for non-production use. The security and privacy of information you provide is not guaranteed.</p>
            </div>
            <form class="form-group" action="/login" method="POST">
              <div class="form-group mb-2">
                <label for="regiser-email">Email address</label>
                <input
                  type="email"
                  class="form-control"
                  id="email"
                  name="email"
                  aria-describedby="emailHelp"
                  placeholder="Enter email"
                  required
                >
              </div>
              <div class="form-group mb-2">
                <label for="regiser-password">Password</label>
                <input
                  type="password"
                  class="form-control"
                  id="password"
                  name="password"
                  aria-describedby="passwordHelp"
                  placeholder="Password"
                  required
                >
              </div>
              <button type="submit" class="btn btn-primary mt-2">Log in</button>
            </form>
            </div>
          </div>
          </div>
        </div>
      </div>
    </main>
    <%- include('partials/_bootstrapJS') %>
  </body>
</html>