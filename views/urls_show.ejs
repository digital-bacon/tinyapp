<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/_bootstrapCSS') %>
    <title>URL - TinyApp</title>
  </head>

  <body>
    <%- include('partials/_header') %>
    <div class="card text-center">
      <div class="card-header"></div>
      <div class="card-body">
        <h5 class="card-title">TinyURL for: <a href="/u/<%= urlId %>" title="Go to <%= longURL %>"><%= longURL %></a></h5>
        <p class="card-text">
          Short URL ID: <%= urlId %>
        </p>
      </div>
      <div class="card-footer text-muted">
        <h3>Edit</h3>
        <form class="form-inline justify-content-center" action="/urls/<%= urlId %>/update" method="POST">
          <div class="form-group mb-2">
            <label for="longURL">New URL:</label>
            <input
              class="form-control"
              type="url"
              name="longURL"
              placeholder="http://"
              pattern="https://.*|http://.*"
              style="width: 300px; margin: 1em"
              value="<%= longURL %>"
              required
            />
            <button type="submit" class="btn btn-primary">Submit</button>
          </div>
        </form>
      </div>
    </div>
    <%- include('partials/_bootstrapJS') %>
  </body>
</html>